# Build standalone calculator.html with run marker

Create a single, standalone file named calculator.html that implements a basic calculator app (addition, subtraction, multiplication, division, clear, and decimal support at minimum). Requirements:

1. The ENTIRE app (HTML structure, CSS styling, and JavaScript logic) must live in this ONE file — no external stylesheets, scripts, or dependencies (no CDN links, no frameworks required).
2. Include an HTML comment near the top of the file containing the exact run marker: <!-- [codex-cost-20260723-183107] -->
3. Do NOT modify, touch, or delete any existing application code, files, or directories in the repo/workspace — this must be a net-new, isolated file only.
4. The calculator should be functional in a standard browser when opened directly (double-click to open, or via a simple static file server) — verify basic operations work (e.g., 2 + 2 = 4, clear button resets display, decimal input works).
5. Keep styling simple and clean (basic CSS is fine — no design system needed).

Acceptance criteria: calculator.html exists as a single file, opens and functions correctly in a browser, contains the exact HTML comment run marker specified above, and no other files in the project were modified.
