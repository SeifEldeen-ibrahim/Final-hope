# Get current weather in Berlin, Germany and save concise result

Use the weather agent to fetch the current weather conditions for Berlin, Germany. Retrieve key details: temperature (actual and feels-like), conditions (e.g. clear, rain, cloudy), humidity, and wind speed. Save the result as a concise summary (2-4 sentences or a short bullet list) suitable for a task log entry — no need for forecasts or historical data, just the current snapshot. Location: Berlin, Germany (default to city-center coordinates if the agent needs precise lat/long).
