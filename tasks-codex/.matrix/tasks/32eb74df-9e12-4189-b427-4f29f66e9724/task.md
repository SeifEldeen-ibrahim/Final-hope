# Build standalone one-file calculator.html app

Create a new, standalone file named calculator.html that implements a basic working calculator (supports digits, decimal point, add/subtract/multiply/divide, clear, and equals/evaluate) using only vanilla HTML, CSS, and JavaScript combined in that single file (inline <style> and <script> tags — no external files, frameworks, or CDN dependencies).

Requirements:
- Single file: calculator.html — must be fully self-contained and open/run directly in a browser with no build step.
- Include an HTML comment near the top of the file containing this exact run marker: <!-- [codex-cost-20260723-174154] Matrix live chat cost audit run marker. No action needed. -->
- UI: a display screen showing current input/result, and clickable buttons for digits 0-9, decimal point, +, -, *, /, =, and a clear (C) button.
- Basic keyboard input support is a nice-to-have but not required.
- Do NOT modify, delete, or touch any existing application files or code in the repo/workspace — this must be a net-new, isolated file only.
- Place the file at the root of the current working directory unless an existing convention for standalone demo/utility files suggests a more appropriate location.
- Acceptance criteria: opening calculator.html in a browser shows a functional calculator that correctly performs basic arithmetic (e.g., 2+2=4, 9/3=3, 5*6=30, 10-4=6), and the file contains the exact run marker comment specified above, and no other files in the workspace show any diff.
