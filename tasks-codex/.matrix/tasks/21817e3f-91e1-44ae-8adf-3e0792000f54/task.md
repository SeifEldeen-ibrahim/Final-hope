# Create standalone one-file calculator.html app

Create a new standalone file named calculator.html containing a simple, functional calculator web app. Requirements:

- Single file only: calculator.html — must contain HTML, CSS, and JavaScript all together in that one file (inline <style> and <script> tags, no external files or dependencies).
- Functionality: basic arithmetic calculator supporting addition, subtraction, multiplication, division, decimal point, clear, and standard button-based numeric input (0-9), with a display screen showing current input/result.
- Must include the following exact run marker as an HTML comment somewhere in the file: <!-- codex-cost-20260723-222856 -->
- Do NOT modify, delete, or touch any existing application files or code in the repo/project — this must be a net-new, additive file only.
- Place the file at the repository root (or the standard location for standalone html demos in this project, if one exists) unless the agent finds a more appropriate existing convention.
- Acceptance criteria: opening calculator.html directly in a browser (no build step, no server required) renders a working calculator UI where basic calculations produce correct results, and the file contains the run marker comment exactly as specified.
