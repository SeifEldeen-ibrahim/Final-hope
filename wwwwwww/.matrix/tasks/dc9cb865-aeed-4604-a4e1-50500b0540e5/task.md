# Get today's current weather for London

Fetch and report the current weather conditions for London, UK using the public weather API. Use coordinates: latitude 51.5074, longitude -0.1278 (Europe/London timezone). Report today's live conditions dynamically at run time (temperature, conditions, wind, etc.) — do NOT hardcode any specific date in the output; always reflect 'today' as of whenever the task executes.
